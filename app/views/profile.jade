extends layout

block content
	if user.firstName
		h1 #{user.firstName}'s Profile
	else
		h1 #{user.email}'s Profile

	h3
		label Email:
		span #{user.email}

	a(href="/") Home
	br
	a(href="/logout") Logout